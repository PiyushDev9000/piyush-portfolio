import React from 'react';

export function AuthButton() {

  
   function handleButtonClick() {
    window.open('https://authapp-tgns.onrender.com/', '_blank');
  }

  return (
    <button
       style={{
    backgroundColor: '#8a2be2',
    color: '#fff',
    padding: '12px 24px',
    fontSize: '0.9rem',
    fontWeight: 'bold',
    marginTop:'8px',
    borderRadius: '4px',
    border: 'none',
    boxShadow: '0px 3px 6px rgba(0, 0, 0, 0.16)',
    cursor: 'pointer',
    transition: 'all 0.3s ease-in-out'
  }}
      onClick={handleButtonClick}
    >
      Link
    </button>
  );
}

export function AiSummerizer() {

  
  function handleButtonClick() {
   window.open('https://sprightly-capybara-883bee.netlify.app/', '_blank');
 }

 return (
   <button
      style={{
   backgroundColor: '#8a2be2',
   color: '#fff',
   padding: '12px 24px',
   fontSize: '0.9rem',
   fontWeight: 'bold',
   marginTop:'8px',
   borderRadius: '4px',
   border: 'none',
   boxShadow: '0px 3px 6px rgba(0, 0, 0, 0.16)',
   cursor: 'pointer',
   transition: 'all 0.3s ease-in-out'
 }}
     onClick={handleButtonClick}
   >
     Link
   </button>
 );
}


export function EcomButton() {

  
  function handleButtonClick() {
    window.open('https://ecommerce-app.cyclic.app/', '_blank');
  }

  return (
    <button
       style={{
    backgroundColor: '#8a2be2',
    color: '#fff',
    padding: '12px 24px',
    marginTop:'8px',
    fontSize: '0.9rem',
    fontWeight: 'bold',
    borderRadius: '4px',
    border: 'none',
    boxShadow: '0px 3px 6px rgba(0, 0, 0, 0.16)',
    cursor: 'pointer',
    transition: 'all 0.3s ease-in-out'
  }}
      onClick={handleButtonClick}
    >
      Link
    </button>
  );
}

export function AppButton() {

  
  function handleButtonClick() {
    window.open('https://expo.dev/%40piyushdev0009/reactmobileapp?serviceType=classic&distribution=expo-go&releaseChannel=default', '_blank');
  }

  return (
    <button
       style={{
    backgroundColor: '#8a2be2',
    color: '#fff',
    padding: '12px 24px',
    fontSize: '0.9rem',
    marginTop:'8px',
    fontWeight: 'bold',
    borderRadius: '4px',
    border: 'none',
    boxShadow: '0px 3px 6px rgba(0, 0, 0, 0.16)',
    cursor: 'pointer',
    transition: 'all 0.3s ease-in-out'
  }}
      onClick={handleButtonClick}
    >
      Link
    </button>
  );
}

export function WeatherButton() {

  
  function handleButtonClick() {
    window.open('https://weather-app-delta-sooty.vercel.app/', '_blank');
  }

  return (
    <button
       style={{
    backgroundColor: '#8a2be2',
    color: '#fff',
    padding: '12px 24px',
    fontSize: '0.9rem',
    fontWeight: 'bold',
    marginTop:'8px',
    borderRadius: '4px',
    border: 'none',
    boxShadow: '0px 3px 6px rgba(0, 0, 0, 0.16)',
    cursor: 'pointer',
    transition: 'all 0.3s ease-in-out'
  }}
      onClick={handleButtonClick}
    >
      Link
    </button>
  );
}

export function ResumeButton() {

  
  function handleButtonClick() {
    window.open('https://drive.google.com/file/d/1RwdgieoETXKNwKgPq8c4D_dm5B-vPq1Y/view?usp=sharing', '_blank');
  }

  return (
    <button
       style={{
    backgroundColor: '#7149c6',
    color: 'white',
    padding: '10px',
    borderRadius: '5px',
    border: 'none',
    cursor: 'pointer',
    width: "100px",
  }}
      onClick={handleButtonClick}
    >
      My Rsume
    </button>
  );
}

export function NextjsblogButton() {

  
  function handleButtonClick() {
    window.open('https://piyushdev-next-js-blog-site.vercel.app/', '_blank');
  }

  return (
    <button
       style={{
    backgroundColor: '#8a2be2',
    color: '#fff',
    padding: '12px 24px',
    fontSize: '0.9rem',
    fontWeight: 'bold',
    marginTop:'20px',
    borderRadius: '4px',
    border: 'none',
    boxShadow: '0px 3px 6px rgba(0, 0, 0, 0.16)',
    cursor: 'pointer',
    transition: 'all 0.3s ease-in-out'
  }}
      onClick={handleButtonClick}
    >
      Link
    </button>
  );
}

